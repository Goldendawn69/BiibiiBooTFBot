Allow users to temporarily block changes to their current form.

Possible commands:

```text
/lockform
/unlockform
```

This is useful if someone likes their current result and does not want it changed.
