### Logging

Add simple command logging.

Possible log file:

```text
logs/bot.log
```

Log useful information only:

```text
timestamp
command name
server ID
user ID
success/failure
```

Avoid logging secrets, tokens, or unnecessary personal details.