If the bot is ever used in more than one server, each server may want different settings.

Possible settings:

```text
allowed channel
cooldown lengths
public/private help
randomtransform enabled/disabled
```