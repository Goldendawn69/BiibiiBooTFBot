### Persistent Cooldowns

Current cooldowns reset when the bot restarts. This is fine for now, but later cooldown timestamps could be stored.

Possible storage options:

```text
users.json
cooldowns.json
SQLite
```