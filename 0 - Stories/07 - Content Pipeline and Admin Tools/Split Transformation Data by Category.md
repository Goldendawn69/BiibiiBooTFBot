### Split Transformation Data by Category

Instead of one large `transformations.json`, split transformation data into category files.

Possible structure:

```text
data/transformations/
  animals.json
  objects.json
  fantasy.json
  robots.json
  mannequins.json
  plushies.json
```

This would make the transformation list easier to maintain.