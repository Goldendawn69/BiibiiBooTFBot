Create a silly server-wide or registered-user event.

Possible command:

```text
/event
```

Example events:

```text
A puff of glitter fills the room. Everyone currently transformed gets a fancy hat.

The magic machine sneezes. One registered user becomes something tiny.

A suspicious sparkle drifts across the server. All plushie forms become extra fluffy for the next hour.
```